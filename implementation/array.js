class MyArray {
    constructor(val) {
      this.storage = new Array(val);
      this.length = val;
    }
    //O(1) unless we've filled the array, then O(n) operation to add to the back of the array
    push(val) {
      

    }
    //O(1) operation to remove a value from the back of the array
    pop() {
   

    }
    //O(n) operation to remove a value from the front of the array
    shift() {


    }
    //O(n) operation to add a value to the front of the array
    unshift(val) {


    }
    //O(n) operation to find the index of a value, returns -1 
    search(val) {


    }
  }
  